/**
 * Job-application “playing card” for marketing: stacked roles, Clearbit logos,
 * horizontal drag + Skip (left) / Apply (right) actions.
 *
 * The heavy WebGL dot-matrix card was not ported here — it adds a large bundle,
 * shader upkeep, and doesn’t match the product’s 2D swipe deck. Use
 * `JobSwipeShowcase` on the landing hero instead.
 */
export { JobSwipeShowcase, type JobSwipeCard } from "./job-swipe-showcase";
